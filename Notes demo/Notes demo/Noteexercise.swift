//
//  Noteexercise.swift
//  Notes demo
//
//  Created by Sara Flora on 21/11/23.
//

import Foundation
/* struct Note {
 var title: String
 
 EVERYTHING MUST HAVE A TYPE : a type annotation
 
 var description:
 
 
 import SwiftUI
 
 struct ContentView: View {
      var thingsToBuy: Note = Note(title: "Buy List", description: "crypto")
      var body: some View {
          List {
             NoteRowView(note: thingsToBuy)
 
 var notes: [Note] = [Note(title: "Note1", description: ""), ]
 
 var body: some View {
     List {

 
 
 import SwiftUI
 
 struct NoteRowView: View {
      var note: Note
      var body: some View {
          VStack {
               Text(note.title)
               Text(note.description)
 }
 }
 }
 
 DATA DRIVEN: WHAT IS VISIBLE FROM THE VIEW



*/
